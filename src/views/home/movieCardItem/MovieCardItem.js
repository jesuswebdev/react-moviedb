import React from 'react';

const MovieCardItem = (props) => {

    //backdropimg, genre, id, original title, title, overview, poster, release date, title, rating

    return (
        <div>

        </div>
    );
}
